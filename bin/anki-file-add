#!/usr/bin/bash

if [ $XDG_SESSION_TYPE = wayland ]
then
	temp=$(wl-paste --primary)
else
	temp=$(xsel -o)
fi

echo $temp | tr '\n' ' '  >> /home/heitor/Documents/Cache/english_phrases.txt
echo '' >> /home/heitor/Documents/Cache/english_phrases.txt
